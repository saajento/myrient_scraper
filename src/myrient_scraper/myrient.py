from myrient_scraper.cache import Cache


class Myrient(Cache):
    pass

#     def __init__(self):
#         self.cache = Cache()

#     def get_url(self, name: str) -> dict:
#         return self.cache.get_url(name)
